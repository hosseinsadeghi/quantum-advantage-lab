// VQE energy-landscape chart — placeholder.
// Will render the per-iteration energy convergence with the exact ground
// state and chemical-accuracy band overlaid, once the VQE race is wired up.

export default function EnergyLandscape() {
  return (
    <div className="viz-placeholder">
      <div className="viz-placeholder-title">Energy Landscape</div>
      <div className="viz-placeholder-body">
        VQE energy convergence — coming soon.
      </div>
    </div>
  );
}
